# crew-server — the unattended fleet host. Its root SSH door is closed like
# workstation's, deliberately departing from every other server definition:
# operators enter as themselves and elevate, while the tailnet join uses an
# auth key rather than waiting for an interactive login.
ROOT_DOOR="closed"
HOST="yes"
JOIN="authkey"
